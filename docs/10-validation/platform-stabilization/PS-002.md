# PS-002 · Canonical Session Validation

**Documento:** `PS-002.md`  
**Fecha:** 2026-07-29  
**Gate:** Platform Stabilization → Flow Certification  
**Contrato:** [FCR-008](../FCR008_CANONICAL_POST_LOGIN_SESSION.md)

---

## Resultado oficial (2026-08-02)

| Variante | Resultado | Notas |
|----------|-----------|--------|
| **PS-002-B** Bootstrap Identity smoke | ✅ PASS *(histórico)* | No demuestra `signInWithPassword` |
| **PS-002-C** Canonical Session (Auth Supabase real) | ✅ **PASS** | Acta [PS002C_PASS_ACTA](./PS002C_PASS_ACTA.md) |

```text
PS-002-C = PASS
duplicates=[] · missing=[] · out_of_order=[]
→ Platform Stabilization COMPLETE (Flow-ready)
→ FLOW CERTIFICATION READY
```

`duration_ms` (diagnóstico): login_to_session=531 · session_to_bootstrap=1 · bootstrap_to_dashboard=833.

No reabrir este gate salvo regresión FCR-008. Siguiente: **FLOW-01** (PR dedicado).

---

## Contrato obligatorio (PS-002-C)

```text
LOGIN
↓
LOGIN_OK
↓
CANONICAL_SESSION
↓
BOOTSTRAP_START
↓
IDENTITY_READY
↓
PROFILE_READY
↓
MEMBERSHIP_READY
↓
ROLE_READY
↓
HOME_PATH_RESOLVED
↓
NAVIGATE
↓
DASHBOARD_RENDERED
```

### Condiciones adicionales

1. **No** `getSession()` inmediato después del login.
2. **Cada evento exactamente una vez** (sin duplicados / bucles).
3. Consola: `[FCR-008]` + `pipelineId`.

Implementación: `src/auth/post-login-pipeline.ts` · runner `npm run test:ps002-canonical-auth`.

---

## Cómo ejecutar PS-002-C

```bash
cp .env.example .env   # una vez · rellena PS002_* y claves Supabase oficiales
npm install
npm run bootstrap:e2e          # Environment Verification (READY / BLOCKED + install si falta)
VITE_BOOTSTRAP_MODE=false npm run dev -- --host 127.0.0.1 --port 8080

# B — smoke (terminal 2) — lee PS002_* desde `.env` (gitignored)
npm run test:ps002-canonical-auth
```

Preflight automático: `.env` · credenciales · Playwright Chromium (`channel: chromium`) · servidor.  
Fallo de entorno → **BLOCKED** con fix explícito (no stack trace).  
Browsers: [PS002C_PLAYWRIGHT_HEADLESS_SHELL.md](../PS002C_PLAYWRIGHT_HEADLESS_SHELL.md).

Plantilla: `.env.example` · secretos solo en `.env` local.  
No mockear Auth.

Opcional: `PS002_ROUTE=/auth/admin` (default) · `PS002_EXPECT_PATH=/admin`.

Evidencia escrita en:

- `evidence/ps002c-canonical-auth.json`
- `evidence/ps002c-after-login.png`

### Forma del reporte (regresión comparable)

```json
{
  "status": "PASS | FAIL | BLOCKED",
  "reason": "",
  "pipeline": [],
  "duplicates": [],
  "missing": [],
  "out_of_order": [],
  "duration_ms": {
    "login_to_session": 87,
    "session_to_bootstrap": 24,
    "bootstrap_to_dashboard": 163
  },
  "code_status": "UNCHANGED"
}
```

| Status | Significado | Exit | Abrir FCR |
|--------|-------------|------|-----------|
| **PASS** | Pipeline completo correcto | 0 | No |
| **FAIL** | Pipeline empezó y se rompió en un paso | 1 | Sí — solo ese paso |
| **BLOCKED** | No pudo empezar (credenciales / server / env) | 2 | **No** |

`duration_ms` es **diagnóstico** (comparar regresiones futuras), no un umbral de rendimiento.  
`PS002_EMAIL` / `PS002_PASSWORD` forman parte del **contrato** del gate — no mockear Auth.

Validador unitario Node: `npm run test:canonical-pipeline`.

### Si falla — comparar pipeline

| Paso               | Esperado | Observado |
| ------------------ | -------- | --------- |
| LOGIN              | ✅        | ?         |
| LOGIN_OK           | ✅        | ?         |
| CANONICAL_SESSION  | ✅        | ?         |
| BOOTSTRAP_START    | ✅        | ?         |
| IDENTITY_READY     | ✅        | ?         |
| PROFILE_READY      | ✅        | ?         |
| MEMBERSHIP_READY   | ✅        | ?         |
| ROLE_READY         | ✅        | ?         |
| HOME_PATH_RESOLVED | ✅        | ✅ |
| NAVIGATE           | ✅        | ✅ |
| DASHBOARD_RENDERED | ✅        | ✅ |

PASS 2026-08-02 — [PS002C_PASS_ACTA](./PS002C_PASS_ACTA.md) · evidencia `evidence/ps002c-canonical-auth.json`.

---

## Criterio FLOW-01

```text
PS-001 = PASS
AND
PS-002-C = PASS (Auth Supabase real · contrato canónico · evidencia)
AND
PS-003 = PASS
↓
PLATFORM STABILIZATION COMPLETE (Flow-ready)
↓
FLOW CERTIFICATION READY
↓
PR FLOW-01 · Kitchen → Delivery · Specification
```

---

## PS-002-B (Bootstrap — no sustituye C)

Smoke histórico bajo Bootstrap. Útil para UI; **insuficiente** para Flow.

## Chequeos estáticos (gates Bootstrap)

| Chequeo | Criterio |
|---------|----------|
| Identity Provider sin `getSession` de mount | FCR-002 intacto |
| Auth submit sin `getSession` post-login | FCR-008 |
| Wiring `PS002_CANONICAL_STEPS` + validador | presente |
